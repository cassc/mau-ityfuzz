
#define PTXSEMA_VERSION "${PTXSEMA_VERSION}"
#define PTXSEMA_VERSION_MAJOR ${PTXSEMA_VERSION_MAJOR}
#define PTXSEMA_VERSION_MINOR ${PTXSEMA_VERSION_MINOR}
#define PTXSEMA_VERSION_PATCH ${PTXSEMA_VERSION_PATCH}

#define LLVM_VERSION "${LLVM_PACKAGE_VERSION}"
#define LLVM_ASSERTIONS "${LLVM_ENABLE_ASSERTIONS}"
#define LLVM_DEBUG ${LLVM_DEBUG}
